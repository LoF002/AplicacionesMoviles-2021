<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.registrousuariosfotografias">

    <uses-permission android:name="android.permission.CALL_PHONE"/>
    <uses-permission android:name="android.permission.CAMERA"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Práctica Registro datos"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RegistroUsuariosFotografias">
        <activity
            android:name=".act_lista_fotos"
            android:exported="true" />
        <activity
            android:name=".act_agregar_fotos"
            android:exported="true" />
        <activity
            android:name=".act_lista_usuarios"
            android:exported="true" />
        <activity
            android:name=".act_registrar_usuarios"
            android:exported="true" />
        <activity
            android:name=".act_login"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>